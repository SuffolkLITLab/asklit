services:
  asklit:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./.streamlit:/app/.streamlit
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: always
